#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @v: the integer to find the absolute value of
 * Return: the absolute value of n
 */
int _abs(int v)
{
	if (v < 0)
		return (-v);
	return (v);
}

